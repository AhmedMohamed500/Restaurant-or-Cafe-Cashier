# معمارية RestaurantFlow

يعتمد المشروع Feature-Based Architecture مع فصل واضح بين العرض والمنطق والتخزين:

```text
app/
  layout.tsx             غلاف التطبيق وRTL والخط
  page.tsx               نقطة دخول الواجهة
src/
  db/
    database.ts          مخطط Dexie وIndexedDB
    seed.ts              البيانات التجريبية والتهيئة
  domain/
    models.ts            نماذج النطاق
    inventory-service.ts معاملات المخزون والتصنيع والبيع
  features/
    app/                 واجهات الوحدات التشغيلية
  lib/
    money.ts             حسابات الكميات والأموال الآمنة
```

## تدفق العملية

1. تحدد الوحدة أصغر أساس للكمية.
2. ترتبط المادة بوحدة أساسية ومرحلة مخزنية.
3. يربط الرصيد بين المادة والمخزن.
4. تحدد الوصفة المدخلات والناتج والهالك.
5. ينفذ أمر التصنيع الخصم والإضافة والحركات داخل Transaction واحدة.
6. تقرأ التكلفة متوسطات تكلفة المواد من طبقة النطاق.
7. تنشئ شاشة الكاشير طلبًا مدفوعًا وتخصم مكونات الوصفة.

لا تعدل الواجهة الأرصدة مباشرة. كل تغيير مالي أو كمي يمر عبر خدمة النطاق ويُسجل في دفتر الحركات.
