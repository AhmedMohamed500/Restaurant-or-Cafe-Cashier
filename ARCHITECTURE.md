# معمارية RestaurantFlow POS

## المبدأ التشغيلي

```text
خارج النظام
   │ إذن إضافة
   ▼
المخزن الرئيسي
   │ تحويل ذري: صرف + استلام
   ▼
المطبخ
   │ تصنيع ذري: استهلاك مكونات + إضافة ناتج
   ▼
المنتج التام
   │ بيع ذري: خصم المنتج فقط
   ▼
العميل
```

الواجهة تفصل دورة التشغيل الخماسية عن الإدارة. الحسابات والموارد البشرية قسمان إداريان مستقلان، بينما الوحدات والوصفات والمخازن والحركات والتكلفة خدمات ونماذج داخلية.

```text
فواتير الكاشير ─┬─> ملخص المبيعات وطرق التحصيل
تكلفة البنود ───┼─> صافي الربح <── مصروفات المطعم
                │
الموظفون ───────┴─> الحضور والانصراف ─> مسير الرواتب
```

## طبقة الترحيل المحاسبي

`src/domain/accounting-service.ts` هو المدخل الوحيد لإنشاء القيود. مكونات React لا تنشئ قيودًا مباشرة. القواعد الرقمية النقية في `src/domain/accounting-rules.ts` تبني أطراف القيد وتتحقق من:

- كل مبلغ بالقروش كعدد صحيح.
- السطر مدين أو دائن فقط.
- عدم وجود قيمة سالبة.
- إجمالي المدين يساوي إجمالي الدائن قبل الحفظ.
- منع تكرار قيد لنفس `referenceType + referenceId`.
- عكس القيود المرحلة بدل تعديلها أو حذفها.

الاستلام والتحويل والتصنيع والبيع يضيف كل منها أثر المخزون والقيد داخل نفس Dexie transaction التي تشمل جداول التشغيل والمحاسبة. لذلك لا يُحفظ نصف الحدث.

## الطبقات

- `src/features/app/RestaurantFlowApp.tsx`: دورة التشغيل، الحسابات، الموارد البشرية، النماذج، الكاشير والإيصال.
- `src/domain/inventory-service.ts`: قواعد الاستلام والتحويل والتصنيع والبيع والمعاملات الذرية.
- `src/domain/accounting-service.ts`: دليل الحسابات، الترحيل المركزي، المشتريات والموردون والنقد والورديات والعكس.
- `src/domain/accounting-rules.ts`: خرائط القيود النقية، التوازن، وملخصات القوائم.
- `src/features/finance/FinanceModule.tsx`: شاشات المالية والتقارير.
- `src/domain/models.ts`: أنواع TypeScript للكيانات والحركات.
- `src/lib/units.ts`: تحويل الوحدات والتحقق من عائلتها.
- `src/lib/money.ts`: تقريب الكميات وحساب الأموال بالقروش.
- `src/lib/auth.ts`: إنشاء بصمة SHA-256 لكلمة المرور المحلية.
- `src/db/database.ts`: جداول Dexie وإصدارات schema والـmigration.
- `src/db/seed.ts`: ضمان وجود الوحدات والمخازن الأساسية دون حذف بيانات المستخدم.

## قواعد الرصيد

- الرصيد هو نتيجة الحركات وليس حقلًا قابلًا للتحرير من الواجهة.
- الكميات داخل `balances.quantity` محفوظة بوحدة الصنف الأساسية.
- حركة المستخدم تحتفظ أيضًا بـ`enteredQuantity` و`enteredUnitId`.
- الوزن والعدد عائلتان منفصلتان.
- متوسط التكلفة ينتقل إلى المطبخ دون تغيير.
- تكلفة المنتج التام هي مجموع تكلفة الكميات المستهلكة من المطبخ مقسومًا على الناتج.

## المعاملات الذرية

### التحويل إلى المطبخ

داخل transaction واحدة: التحقق من الرصيد، خصم المصدر، إضافة الوجهة، ثم إنشاء حركتي `transfer_to_kitchen_out` و`transfer_to_kitchen_in` بنفس المرجع.

### التصنيع

داخل transaction واحدة: تحميل الوصفة، تحويل وحدات المكونات، التحقق من كل الأرصدة، خصم المطبخ، حساب التكلفة، إضافة المنتج التام، إنشاء الحركات، وحفظ أمر الإنتاج.

تُطبّع مكونات الوزن إلى الجرام عند حفظ الوصفة وقبل تنفيذها. يحتفظ أمر التصنيع بالكمية المدخلة كجرامات، بينما يظل تحويل الأرصدة الداخلية متوافقًا مع الوحدة الأساسية للصنف. الوصفات القديمة المكتوبة بالكيلوجرام تتحول تلقائيًا إلى جرام عند تهيئة التطبيق.

### البيع

داخل transaction واحدة: التحقق من الوردية ورصيد المنتج التام، خصم المنتجات، إنشاء حركات `finished_product_sale`، وحفظ الفاتورة. الوصفة ليست جزءًا من البيع ولا تُخصم الخامات مرة ثانية.

بهذا يكون الخصم مرة واحدة فقط: مكونات الوصفة بالجرام عند التصنيع، ثم عدد المنتج التام عند البيع.

### المشتريات والموردون

فاتورة المشتريات تزيد `balances` وتضيف `movements` بالقيمة والوحدة نفسها، ثم ترحل مخزون المواد الخام وضريبة المدخلات مقابل النقد/البنك أو الموردين. المرتجع يعكس المخزون والقيد، وسداد المورد يخفض حساب الدائنين مقابل حساب الدفع المختار.

### النقد والورديات

التحويل بين حسابي نقد أو بنك قيد أصول داخلي. عند إغلاق الوردية تجمع المبيعات حسب طريقة الدفع منذ وقت الفتح، وتحسب النقد المتوقع والفرق، ولا تنشئ قيد عجز أو فائض عندما يكون الفرق صفرًا.

## التوافق مع البيانات القديمة

أبقينا `raw` و`work_in_progress` و`finished` كمراحل داخلية. Schema v2 يضيف فهارس المصدر والوجهة، وSchema v3 يضيف السجلات المالية المبسطة والموارد البشرية، وSchema v4 يضيف محرك ERP المحاسبي دون مسح الجداول. التهيئة تضيف فقط الوحدات والمخازن والحسابات النظامية المفقودة.

## العلامة التجارية وقفل الدخول المحلي

يحفظ جدول `settings` اسم المطعم والشعار واسم المستخدم وبصمة كلمة المرور. لا تُخزن كلمة المرور الخام. `sessionStorage` يحمل حالة الجلسة الحالية فقط، بينما تظل الإعدادات في IndexedDB. هذا القفل مناسب للنسخة المحلية المجانية، وليس بديلًا عن مصادقة خادمية متعددة الأجهزة.

## الرقابة التشغيلية

`operations-service.ts` هو المصدر الوحيد لاعتماد الجرد وتسجيل الهالك وإغلاق الوردية. كل عملية مؤثرة تنفذ داخل Dexie transaction وتشمل الرصيد والحركة والقيد والتنبيه وسجل المراجعة. يحتوي `operations-rules.ts` الحسابات النقية للنقدية المتوقعة وفروق الجرد وتكلفة الطعام.
