# معمارية RestaurantFlow POS

## المبدأ التشغيلي

```text
خارج النظام
   │ إذن إضافة
   ▼
المخزن الرئيسي
   │ تحويل ذري: صرف + استلام
   ▼
المطبخ
   │ تصنيع ذري: استهلاك مكونات + إضافة ناتج
   ▼
المنتج التام
   │ بيع ذري: خصم المنتج فقط
   ▼
العميل
```

الواجهة تعرض خمس خطوات فقط. الوحدات والوصفات والمخازن والحركات والتكلفة ما زالت خدمات ونماذج داخلية تدعم الحسابات، وليست أقسام تشغيل مستقلة.

## الطبقات

- `src/features/app/RestaurantFlowApp.tsx`: الواجهة الخماسية والنماذج والكاشير والإيصال.
- `src/domain/inventory-service.ts`: قواعد الاستلام والتحويل والتصنيع والبيع والمعاملات الذرية.
- `src/domain/models.ts`: أنواع TypeScript للكيانات والحركات.
- `src/lib/units.ts`: تحويل الوحدات والتحقق من عائلتها.
- `src/lib/money.ts`: تقريب الكميات وحساب الأموال بالقروش.
- `src/db/database.ts`: جداول Dexie وإصدارات schema والـmigration.
- `src/db/seed.ts`: ضمان وجود الوحدات والمخازن الأساسية دون حذف بيانات المستخدم.

## قواعد الرصيد

- الرصيد هو نتيجة الحركات وليس حقلًا قابلًا للتحرير من الواجهة.
- الكميات داخل `balances.quantity` محفوظة بوحدة الصنف الأساسية.
- حركة المستخدم تحتفظ أيضًا بـ`enteredQuantity` و`enteredUnitId`.
- الوزن والعدد عائلتان منفصلتان.
- متوسط التكلفة ينتقل إلى المطبخ دون تغيير.
- تكلفة المنتج التام هي مجموع تكلفة الكميات المستهلكة من المطبخ مقسومًا على الناتج.

## المعاملات الذرية

### التحويل إلى المطبخ

داخل transaction واحدة: التحقق من الرصيد، خصم المصدر، إضافة الوجهة، ثم إنشاء حركتي `transfer_to_kitchen_out` و`transfer_to_kitchen_in` بنفس المرجع.

### التصنيع

داخل transaction واحدة: تحميل الوصفة، تحويل وحدات المكونات، التحقق من كل الأرصدة، خصم المطبخ، حساب التكلفة، إضافة المنتج التام، إنشاء الحركات، وحفظ أمر الإنتاج.

### البيع

داخل transaction واحدة: التحقق من الوردية ورصيد المنتج التام، خصم المنتجات، إنشاء حركات `finished_product_sale`، وحفظ الفاتورة. الوصفة ليست جزءًا من البيع ولا تُخصم الخامات مرة ثانية.

## التوافق مع البيانات القديمة

أبقينا `raw` و`work_in_progress` و`finished` كمراحل داخلية. Schema v2 يضيف فهارس المصدر والوجهة ويكمل الحقول الجديدة للحركات القديمة دون مسح الجداول. التهيئة تضيف فقط الوحدات أو المخازن النظامية المفقودة.
