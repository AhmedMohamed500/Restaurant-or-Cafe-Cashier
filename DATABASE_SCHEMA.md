# مخطط قاعدة بيانات RestaurantFlow POS

قاعدة البيانات `RestaurantFlowPOS` تعمل بـDexie فوق IndexedDB. الإصدار الحالي: **2**.

## الجداول

### `units`

وحدات القياس الداخلية: `id`, `code`, `nameAr`, `nameEn`, `symbol`, `family`, `baseFactor`, `active` وحقول التدقيق. الوحدات النظامية هي جرام وكيلوجرام وعدد.

### `items`

المواد والمنتجات: الكود والأسماء والتصنيف والمرحلة والوحدة الأساسية ووحدة الشراء والحد الأدنى ومتوسط التكلفة بالقروش وسعر البيع والصورة والحالة.

### `warehouses`

مواقع المراحل الثلاث:

- `raw`: المخزن الرئيسي.
- `work_in_progress`: المطبخ.
- `finished`: المنتج التام.

### `balances`

رصيد مركب بالمفتاح `[warehouseId+itemId]`: الكمية الأساسية، المحجوز، متوسط التكلفة بالقروش، ووقت آخر تحديث.

### `movements`

دفتر الحركات غير القابل للتعديل:

- `warehouseId`, `itemId`, `type`, `reference`.
- `quantity`: كمية موجبة أو سالبة بالوحدة الأساسية.
- `enteredQuantity`, `enteredUnitId`: إدخال المستخدم الأصلي.
- `sourceWarehouseId`, `destinationWarehouseId`: مسار التحويل.
- `unitCostPiasters`, `totalCostPiasters`, `note` وحقول التدقيق.

أنواع التشغيل الجديدة: `stock_receipt`, `transfer_to_kitchen_out`, `transfer_to_kitchen_in`, `production_consume`, `production_output`, `finished_product_sale`. الأنواع القديمة باقية لقراءة البيانات التاريخية.

### `recipes`

تعريف داخلي لأمر التصنيع: المنتج الناتج وكميته ووحدته وسعره ومكوناته. كل مكون يحمل الكمية والوحدة والهالك والاختيارية.

### `productionOrders`

رقم الأمر والوصفة والكميات المخططة والفعلية ومخزن المطبخ ومخزن المنتج التام والتكلفة والهالك والحالة.

### `saleOrders`

الفواتير المدفوعة: رقم الطلب، النوع، البنود، الإجمالي الفرعي، الضريبة، الإجمالي، طريقة الدفع، والحالة.

### `settings`

اللغة والمظهر وحالة الوردية وعلامة التهيئة.

## Migration من الإصدار 1 إلى 2

- لا تمسح أي جدول.
- تضيف فهارس مصدر ووجهة الحركة.
- تضيف `enteredQuantity` للحركات القديمة من القيمة المطلقة للكمية.
- تستنتج المصدر للحركة السالبة والوجهة للحركة الموجبة عندما لا تكون الحقول موجودة.
- تظل جميع الحقول الجديدة اختيارية عند قراءة السجلات القديمة.

## قواعد الدقة

- الأموال بالقروش كأعداد صحيحة.
- الكميات مقربة إلى ثلاث منازل عشرية.
- التحويل يتم بواسطة `(quantity × fromFactor) ÷ toFactor` داخل نفس عائلة الوحدة.
