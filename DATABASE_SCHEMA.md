# مخطط البيانات المحلي

| الجدول | الغرض | المفاتيح المهمة |
|---|---|---|
| `units` | وحدات القياس | `id`, `code`, `family` |
| `items` | المواد والمنتجات | `id`, `code`, `stage` |
| `warehouses` | مواقع التخزين | `id`, `code`, `stage` |
| `balances` | الرصيد الحالي | `[warehouseId+itemId]` |
| `recipes` | الوصفات ومكوناتها | `id`, `outputItemId` |
| `movements` | دفتر حركات المخزون | `itemId`, `type`, `reference` |
| `productionOrders` | أوامر التصنيع المكتملة | `number`, `recipeId` |
| `saleOrders` | فواتير البيع | `number`, `createdAt` |
| `settings` | الإعدادات المحلية | `id` |

العلاقات الأساسية:

- كل مادة لها وحدة أساسية ووحدة شراء.
- كل رصيد يخص مادة واحدة داخل مخزن واحد.
- كل وصفة تنتج مادة وتحتوي أي عدد من المكونات.
- أمر التصنيع يرتبط بوصفة وحركات صرف وإضافة من خلال رقم المرجع.
- فاتورة البيع تحتوي بنودًا، وكل بند قد يستهلك مكونات وصفته.

جميع القيم المالية تُخزن بالقروش كأعداد صحيحة. الكميات تقرّب إلى ثلاث منازل عشرية، وتستخدم العمليات الحرجة معاملات IndexedDB الذرّية.
