"use client";
import {db} from "@/src/db/database";import type {GoodsReceipt,GoodsReceiptLine,ProcurementOrder,ProcurementOrderLine,PurchaseRequest,PurchaseRequestLine,SupplierInvoiceRecord,SupplierInvoiceRecordLine,UserRole} from "./models";import {requirePermission} from "./authorization-service";import {requestApproval} from "./approval-service";import {addOpeningStock} from "./inventory-service";import {createJournal} from "./accounting-service";
const uid=()=>crypto.randomUUID(),now=()=>new Date().toISOString(),audit=(user:string)=>({createdAt:now(),updatedAt:now(),createdBy:user});const num=async(prefix:string,table:{count:()=>Promise<number>})=>`${prefix}-${String(await table.count()+1).padStart(6,"0")}`;
export async function createPurchaseRequest(input:{role:UserRole;user:string;requiredDate:string;warehouseId:string;notes?:string;lines:Array<Omit<PurchaseRequestLine,"id"|"purchaseRequestId">>}){await requirePermission(input.role,"purchases.request");return db.transaction("rw",[db.purchaseRequests,db.purchaseRequestLines,db.auditLogs],async()=>{const row:PurchaseRequest={id:uid(),number:await num("PR",db.purchaseRequests),requestDate:now().slice(0,10),requestedBy:input.user,requiredDate:input.requiredDate,warehouseId:input.warehouseId,notes:input.notes,status:"draft",...audit(input.user)};await db.purchaseRequests.add(row);await db.purchaseRequestLines.bulkAdd(input.lines.map(x=>({...x,id:uid(),purchaseRequestId:row.id})));await db.auditLogs.add({id:uid(),action:"purchase_request_create",entityType:"purchase_request",entityId:row.id,reference:row.number,timestamp:now(),localUser:input.user,afterSummary:`lines=${input.lines.length}`});return row;});}
export async function submitPurchaseRequest(id:string,user:string){const row=await db.purchaseRequests.get(id);if(!row||row.status!=="draft")throw new Error("طلب الشراء غير قابل للإرسال");await db.transaction("rw",[db.purchaseRequests,db.approvals],async()=>{await db.purchaseRequests.update(id,{status:"pending_approval",updatedAt:now()});await requestApproval({entityType:"purchase_request",entityId:id,reference:row.number,requestedBy:user});});}
export async function createPurchaseOrder(input:{role:UserRole;user:string;requestId?:string;supplierId:string;warehouseId:string;expectedDeliveryDate:string;paymentTerms?:string;notes?:string;lines:Array<Omit<ProcurementOrderLine,"id"|"purchaseOrderId"|"receivedQuantity"|"lineTotalPiasters">>}){await requirePermission(input.role,"purchases.create_order");return db.transaction("rw",[db.procurementOrders,db.procurementOrderLines,db.purchaseRequests,db.auditLogs],async()=>{let subtotal=0,discount=0,vat=0;const id=uid(),lines=input.lines.map(x=>{const before=x.orderedQuantity*x.unitPricePiasters,net=before-x.discountPiasters,tax=Math.round(net*x.vatRate/100);subtotal+=before;discount+=x.discountPiasters;vat+=tax;return{...x,id:uid(),purchaseOrderId:id,receivedQuantity:0,lineTotalPiasters:net+tax}});const row:ProcurementOrder={id,number:await num("PO",db.procurementOrders),requestId:input.requestId,supplierId:input.supplierId,orderDate:now().slice(0,10),expectedDeliveryDate:input.expectedDeliveryDate,warehouseId:input.warehouseId,paymentTerms:input.paymentTerms,notes:input.notes,status:"draft",subtotalPiasters:subtotal,discountPiasters:discount,vatPiasters:vat,totalPiasters:subtotal-discount+vat,...audit(input.user)};await db.procurementOrders.add(row);await db.procurementOrderLines.bulkAdd(lines);if(input.requestId)await db.purchaseRequests.update(input.requestId,{status:"converted",updatedAt:now()});return row;});}
export async function approvePurchaseOrder(id:string,role:UserRole,user:string){await requirePermission(role,"purchases.approve_order");const row=await db.procurementOrders.get(id);if(!row)throw new Error("أمر الشراء غير موجود");await db.procurementOrders.update(id,{status:"approved",updatedAt:now()});await db.auditLogs.add({id:uid(),action:"purchase_order_approve",entityType:"purchase_order",entityId:id,reference:row.number,timestamp:now(),localUser:user});}
export async function postGoodsReceipt(input:{role:UserRole;user:string;purchaseOrderId:string;notes?:string;lines:Array<{purchaseOrderLineId:string;quantity:number}>}){await requirePermission(input.role,"purchases.receive");const po=await db.procurementOrders.get(input.purchaseOrderId);if(!po||!["approved","partially_received"].includes(po.status))throw new Error("أمر الشراء غير معتمد");const poLines=await db.procurementOrderLines.where("purchaseOrderId").equals(po.id).toArray();const receiptId=uid(),number=await num("GRN",db.goodsReceipts);return db.transaction("rw",[db.goodsReceipts,db.goodsReceiptLines,db.procurementOrders,db.procurementOrderLines,db.units,db.items,db.warehouses,db.balances,db.movements,db.accounts,db.journalEntries,db.journalLines,db.auditLogs],async()=>{const saved:GoodsReceiptLine[]=[];for(const requested of input.lines){const line=poLines.find(x=>x.id===requested.purchaseOrderLineId);if(!line||requested.quantity<=0||line.receivedQuantity+requested.quantity>line.orderedQuantity)throw new Error("كمية الاستلام تتجاوز المتبقي في أمر الشراء");await addOpeningStock({warehouseId:po.warehouseId,itemId:line.itemId,quantity:requested.quantity,enteredUnitId:line.unitId,unitCostPiasters:line.unitPricePiasters,reference:number,note:`استلام ${po.number}`,skipAccounting:true});await db.procurementOrderLines.update(line.id,{receivedQuantity:line.receivedQuantity+requested.quantity});saved.push({id:uid(),goodsReceiptId:receiptId,purchaseOrderLineId:line.id,itemId:line.itemId,quantity:requested.quantity,unitId:line.unitId,unitCostPiasters:line.unitPricePiasters});}const receipt:GoodsReceipt={id:receiptId,number,purchaseOrderId:po.id,supplierId:po.supplierId,warehouseId:po.warehouseId,receiptDate:now().slice(0,10),receivedBy:input.user,notes:input.notes,status:"posted",...audit(input.user)};await db.goodsReceipts.add(receipt);await db.goodsReceiptLines.bulkAdd(saved);const updated=await db.procurementOrderLines.where("purchaseOrderId").equals(po.id).toArray();await db.procurementOrders.update(po.id,{status:updated.every(x=>x.receivedQuantity>=x.orderedQuantity)?"fully_received":"partially_received",updatedAt:now()});await db.auditLogs.add({id:uid(),action:"goods_receipt_post",entityType:"goods_receipt",entityId:receipt.id,reference:number,timestamp:now(),localUser:input.user,afterSummary:`po=${po.number};lines=${saved.length}`});return receipt;});}
export async function postSupplierInvoice(input:{user:string;supplierInvoiceNumber:string;supplierId:string;invoiceDate:string;dueDate:string;purchaseOrderId?:string;goodsReceiptId?:string;notes?:string;lines:Array<Omit<SupplierInvoiceRecordLine,"id"|"supplierInvoiceId"|"totalPiasters">>}){return db.transaction("rw",[db.supplierInvoiceRecords,db.supplierInvoiceRecordLines,db.goodsReceiptLines,db.accounts,db.journalEntries,db.journalLines,db.auditLogs],async()=>{let subtotal=0,discount=0,vat=0;const id=uid(),received=input.goodsReceiptId?await db.goodsReceiptLines.where("goodsReceiptId").equals(input.goodsReceiptId).toArray():[];let quantityDifference=false;const lines=input.lines.map(x=>{const net=x.quantity*x.unitCostPiasters-x.discountPiasters,tax=Math.round(net*x.vatRate/100);subtotal+=x.quantity*x.unitCostPiasters;discount+=x.discountPiasters;vat+=tax;if(!received.some(r=>r.itemId===x.itemId&&r.quantity>=x.quantity))quantityDifference=true;return{...x,id:uid(),supplierInvoiceId:id,totalPiasters:net+tax}});const total=subtotal-discount+vat,number=await num("PINV",db.supplierInvoiceRecords);const row:SupplierInvoiceRecord={id,number,supplierInvoiceNumber:input.supplierInvoiceNumber,supplierId:input.supplierId,invoiceDate:input.invoiceDate,dueDate:input.dueDate,purchaseOrderId:input.purchaseOrderId,goodsReceiptId:input.goodsReceiptId,subtotalPiasters:subtotal,discountPiasters:discount,vatPiasters:vat,totalPiasters:total,paidPiasters:0,matchingStatus:quantityDifference?"quantity_difference":input.goodsReceiptId?"matched":"unmatched",status:"unpaid",notes:input.notes,...audit(input.user)};await db.supplierInvoiceRecords.add(row);await db.supplierInvoiceRecordLines.bulkAdd(lines);await createJournal({date:row.invoiceDate,referenceType:"supplier_invoice",referenceId:id,referenceNumber:number,description:"إثبات فاتورة مورد",sourceModule:"purchasing",lines:[{accountCode:"1210",debitMinor:subtotal-discount,creditMinor:0,description:"قيمة المواد المستلمة"},{accountCode:"2190",debitMinor:vat,creditMinor:0,description:"ضريبة مدخلات"},{accountCode:"2110",debitMinor:0,creditMinor:total,description:"الموردون"}].filter(x=>x.debitMinor||x.creditMinor)});return row;});}
